import type { Metadata } from "next";
import { HomeHealthcarePage } from "@/components/ServicePages";
export const metadata:Metadata={title:"Home Healthcare | MASSH",description:"MASSH home healthcare, elderly, critical, palliative and attendant care services."};
export default function Page(){return <HomeHealthcarePage/>}
